"""Per-destination page widgets.

For now every destination renders a placeholder; real pages are migrated
one at a time following the order in the rewrite plan (Image -> Video ->
multi-input -> PDF -> rest -> Queue/Settings/About).
"""
