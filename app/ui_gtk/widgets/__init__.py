"""Reusable UI building blocks for the GTK4 interface.

These components encode the house style once so every converter page
assembles from the same parts: a drop zone, a format picker, a quality
control, an output-destination row, a preset bar, and the page scaffold
(clamped preferences page + footer action bar).
"""
