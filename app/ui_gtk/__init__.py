"""GTK4 / libadwaita presentation layer for T-Rex Converter.

This package is the in-progress rewrite of the UI from PySide6/Qt
(``app.ui``) to GTK 4 + libadwaita (PyGObject). The backend layers
(``app.core``, ``app.engines``, ``app.data``, ``app.utils``) are shared
unchanged. See the design docs for navigation, page anatomy and the
house style that this code must follow.
"""

APP_ID = "io.github.s4rt4.trexconverter"
